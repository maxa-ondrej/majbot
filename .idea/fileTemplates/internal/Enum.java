#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")
/**
 * <p><b>Enum {@link ${NAME}}</b></p>
 *
 * @author ${USER}
 * @version ${VERSION}
 * @since ${VERSION}
 */
public enum ${NAME} {
}
